# CoreChat AI — environment variables
# Copy to .env.local and fill in.

# n8n webhook URL that receives chat requests and returns { "reply": "..." }
VITE_N8N_WEBHOOK_URL=

# Optional REST API base for any other backend services
VITE_API_BASE_URL=
